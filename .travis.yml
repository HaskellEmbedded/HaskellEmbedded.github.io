# The below just generates the site with Hakyll.  It does not (yet) commit and
# push the generated site anywhere.

language: haskell
ghc: 7.8
branches:
  only:
  - source
script:
  - cabal configure --enable-tests && cabal build && cabal run build
# Handle git submodules yourself
git:
  submodules: false
# Use sed to replace the SSH URL with the public URL, then initialize submodules
before_install:
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init

# TODO:
# http://docs.travis-ci.com/user/caching/#Arbitrary-directories
# (Perhaps run in a cabal sandbox, and cache the contents of the sandbox.)
# http://begriffs.com/posts/2014-08-12-create-static-site-with-hakyll-github.html
# Whatever is at the above to let this actually commit.